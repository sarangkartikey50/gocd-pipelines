format_version: 3
pipelines:
  node-ci:
    group: NODEJS-GOCD
    materials:
      node-pr-listener:
        options:
          url: git@github.com:sarangkartikey50/nodejs-github-actions.git
        autoUpdate: false
        shallowClone: true
        branch: master
    stages:
      - install-dependencies:
          clean_workspace: true
          jobs:
            lint-checks:
              tasks:
                - exec:
                    command: bash
                    arguments:
                      - "-c"
                      - "npm i"
                - exec:
                    command: bash
                    arguments:
                      - "-c"
                      - "npm run lint"